- name: Call worker endpoint
  run: |
    echo "Calling: ${{ secrets.WORKER_URL }}"
    curl -i -sS -X POST \
      -H "x-worker-secret: ${{ secrets.WORKER_SECRET }}" \
      "${{ secrets.WORKER_URL }}"